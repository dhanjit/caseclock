# Normalize line endings to LF in the repo (this machine uses core.autocrlf=false).
* text=auto eol=lf

# Binary assets
*.png binary
*.jpg binary
*.wasm binary
*.apk binary
*.keystore binary
*.jks binary
